# Durable image publication — fresh sandbox or blocked legacy cutover

The localhost acceptance client is archived under `scripts/fixtures/`, outside the deployed app.
The local VM regression reads that copy. It does not require files in `public/`.
For approved live sandbox checks, copy both `sandbox-image-publication-acceptance.*` files into the isolated sandbox server's `public/` directory.
Do not include those pages in a deployment. The client still requires the exact localhost origin and sandbox project below.

## Fresh sandbox: exact next action and apply path

**Next user action:** in sandbox project **pwpklwihwmxdehfdsoij**, create a **new private** Storage bucket named exactly **`capture-image-candidates-fresh-20260914`**, maximum **2250000 bytes**, allowed MIME exactly **`image/png`, `image/jpeg`, `image/webp`, `image/gif`**. Do not upload anything, create policies/capabilities for it, or delete/recreate this name. If this name has ever existed, STOP rather than reuse it. Keep `capture-images` and `capture-image-candidates` untouched.

This is the approved disposable-fixture sandbox path, **not legacy cutover and not production approval**. Old images remain physically present but **fresh mode intentionally does not serve them**. HEAD/GET without a publication return 404; PUT starts a new UUID candidate even if the same logical ID exists in the old bucket. There is no legacy Storage read, collision lookup, drain assertion, freeze, copy or deletion in fresh mode. Locally retained browser bytes can be uploaded again after HEAD404; missing local bytes are not recovered from the old bucket.

After parent review, the user/operator runs:

1. Review `supabase/sandbox-image-publication-fresh-apply.sql` — **instead of** publication migration `20260913220000` and legacy stage 2. This single transaction targets the stated current sandbox state: billing and exactly the eight original image policies already installed; no publication tables/functions installed. Do not run `supabase db push`. Do not apply old stage 1 first.
2. Fill only `namespace_reference` and `operator_name` in the reviewed copy. Blank fields deliberately abort. The report must establish this exact bucket identity has never existed before, was provisioned private with exact controls, and had **no earlier writers or signed/upload capabilities (including privileged callers)** from provisioning through activation. Confirm the sandbox/project in the dashboard. A currently empty listing is NOT namespace-history proof. SQL checks known scoped policies and RLS, exact bucket controls, zero candidate rows, and zero newly created publication rows immediately before activation. It cannot authenticate an operator report or reconstruct deleted bucket history. If provenance cannot be established, leave inactive and do not invent evidence.
3. Run the filled fresh apply file, then `supabase/verify-image-publication-fresh.sql` — must return active config `{"mode":"fresh","bucket":"capture-image-candidates-fresh-20260914"}` and PASS. Record output. No route environment change is needed: both readiness and configuration come from protected DB state; missing/mismatched mode/bucket fails 503. No request-selected mode, new project, key or deployment is involved.
4. At **http://127.0.0.1:4998/sandbox-image-publication-acceptance.html**, with an already paid synthetic sandbox session, click **Run five publication acceptance rounds** once. Require one true/one false and stable attributed bytes in every round; retain results and use the reread button. **The agent has not applied SQL or run this live batch.** Only this user-run step can provide hosted race evidence.

Activation is default absent/inactive and one-way: immutable app-owned record binds mode, bucket name plus creation timestamp, reviewed provenance and initial empty assertion. Empty state is checked **once**, not required after successful publication. Ongoing readiness checks bucket identity/controls, RLS, activation trigger and fixed fresh policy fingerprint. Supported restrictive Storage RLS protects candidates; no managed-table triggers/ALTER or Storage SQL writes occur. Only app-owned tables have triggers. Admin Storage API cleanup remains possible; disable image availability at the app/deployment boundary when investigating, not by rewriting activation or recycling bucket identity.

Before production, obtain authorized actual deployment/project/flag inventory, Cloud boards/revisions, subscription/account usage, legacy objects, publication rows and namespace history. “Cloud never enabled” or no currently paid users does not prove no recovery obligations. If obligations exist, fresh abandonment is not authorized. Legacy-cutover evidence remains separately blocked below, and reliable in-flight account erasure remains a production gate for either mode.

Local fresh verification: `python3 scripts/test-image-publication-sql.py --fresh` exercises a real disposable socket-only PostgreSQL cluster. It rejects blank evidence, unknown policies, populated candidates and reused setup; enforces immutable mode/bucket; races five pairs of publications alongside late old-bucket writes; checks paid/tenant/operation restrictions and readiness drift. Route tests cover fresh no-fallback, mode mismatch, independent concurrent winners and crash retry. These are local fixtures, not namespace provenance, hosted Storage bytes or drain evidence.

## Legacy-cutover status / scope (separate, still blocked)

Revised locally; **not applied to hosted Supabase and not deployed**. No remote writes, credentials, billing, notes or user photos accessed. Acceptance page/script unchanged. The rejected provider-table trigger is removed, not replaced with an ownership or trigger-mode workaround. Stage 1 is an admissions closure, not a completed migration of legacy data.

The new route deliberately returns **503** for paid image operations until the migration readiness RPC is installed and returns true. It never falls back to the known-broken same-key Storage upload path. Unpaid requests still return 402 and anonymous requests 401. Do not deploy this route independently and call that a working hosted rollout.

## Protocol

- Preserve `/api/img/<logical-id>` and existing `capture-images/<user>/<logical-id>` objects.
- Close legacy admissions using restrictive RLS, then keep activation off until a verified drain and full legacy-preservation evidence exist. Only AFTER that gate, retained legacy objects remain readable; duplicate PUT returns `stored:false` without an upload or publication row. Current local tests do not establish hosted legacy preservation.
- New attempts upload once to the separate private **capture-image-candidates** bucket under `<verified-user>/<random-UUID>`. Never reuse a candidate path, even after an uncertain upload response.
- Validate downloaded candidate bytes before publication. Plain INSERT into `capture_image_publications`, keyed by `(user_id,image_id)`, arbitrates across processes. SQLSTATE 23505 means read the winner, not retry an overwrite/upsert.
- Publication records bind UUID, SHA-256, MIME and length. Read back the durable pointer and exact candidate bytes before acknowledging either winner or loser. A missing/corrupt published object returns 503, never 404 or permission to upload a replacement.
- No per-request delete/cleanup calls. Failed and losing candidates remain. A crash before INSERT leaves an orphan and retries with a fresh UUID. A commit with a lost response is recovered from the pointer and returns `stored:false`. Administrative account erasure is supported separately below.
- HEAD uses metadata-only `info()` (size/MIME and published pointer checks), no image download. **204 means metadata existence, not integrity or recoverability.** Same-size/same-MIME corruption can pass HEAD, suppressing retries, while GET and PUT fail 503. This explicitly accepts owner-caused availability loss to restore cheap reload checks. A dangling publication is 503, never 404. GET and EVERY PUT acknowledgment download/validate raster bytes and publication digest/MIME/length; new PUT additionally validates before INSERT. Legacy objects lack historical digests and are safe only under the verified no-writer cutover assumption.

## Why activation remains blocked after admissions close

The pinned upstream uploader admits requests with a rolled-back user permission probe, then completes via a superuser upsert. A restrictive old-bucket INSERT policy alone cannot stop an upload admitted before policy installation.

Stage 1 commits restrictive INSERT denial for legacy paths, covering old app routes and direct Storage admissions, while retaining old SELECT/UPDATE/DELETE restrictions. Candidate admissions and direct publication INSERTs are also closed until operator attestation. No managed-table trigger, lock, ownership alteration, trigger-mode change, or Storage metadata mutation is used. The only trigger belongs to the app-owned publication table and prevents an owner from shadowing an existing legacy ID, including IDs completed during drain.

**No provider-documented, hosted-verified bound/procedure covering all pre-admitted completions was found in the reviewed sources.** Supabase documents [TUS URL expiry up to 24 hours](https://supabase.com/docs/guides/storage/uploads/resumable-uploads#upload-url), but that is NOT a bound on standard uploads, already-running finalization, signed uploads or queued cleanup. Do not use 24 hours, the app's 60-second timeout, SQL locks, an empty activity view, or repeated quiet listings as a drain proof. The stage-1 timestamp is recorded before commit and is only a lower bound; evidence must identify successful commit and the subsequent denial probes.

The pinned [upstream uploader](https://github.com/supabase/storage/blob/755986d5a8d915296d0fedafe03011e2616ae563/src/storage/uploader.ts) motivates the race model; it is not hosted deployment provenance. Required evidence: provider procedure/version covering every enabled upload route, resumption/token behavior, finalization and cleanup; actual execution/completion after admissions closed; then a complete owner/path/MIME/size/digest legacy inventory and independent byte readback. Secure evidence outside the browser; SQL stores report IDs only. Existing legacy bytes have no historical digest: preservation means a verified post-drain snapshot, not recovery of a pre-race original.

**Safest decision now: leave stage 2 inactive and do not deploy/enable the new route.** Ask the provider one bounded question for that drain procedure. If unavailable, stop before more infrastructure: choose a separately approved content-pinned inventory/copy design with exhaustive inventory, fixed snapshot semantics, copies independently hashed, durable reservations for every legacy ID and handling of late arrivals/missing originals. That design is NOT implemented here, and a copy loop alone does not establish preservation. Explicitly approved disposable sandbox reset can remove sandbox preservation obligations only; it does not prove production preservation or drain old uploads. No reset is authorized or implemented by these files.

## Security / direct-Storage boundary

- User identity comes from verified claims and `X-Capture-Owner` must match. Both publication RLS and Storage policies require the same live paid entitlement; no app service role.
- Restrictive candidate policies survive unrelated permissive policies, scope paths to the user and a UUID, disallow anon, listing/signing/copy, signed uploads, updates and deletion. Legacy policies stay in place with an additional insert freeze.
- The publication table grants authenticated SELECT/INSERT only. The legacy guard has fixed search_path and checks owner and cutover before its privileged metadata existence read. The private app-owned cutover table has RLS and no public/anon/authenticated grants. Boolean RPCs expose no evidence/user data. Nonempty operator evidence is an administrative attestation, NOT automatic verification that evidence is true. Catalog checks are drift alarms, not semantic verification of policy definitions.
- The application still does **not** repair the provider's general create-only API. Application UUIDs are unguessable, fresh and never reused. A hostile same-owner direct client can deliberately race the SAME candidate path, overwrite through pre-admitted provider completions, insert bad pointers or select its own digest. It can self-deny availability, including after an earlier success. GET and PUT fail closed on any published-byte mismatch; HEAD is existence only. This is not a tamper-proof backup against an owner, nor a signed proof of app authorship. Tenant paths and paid RLS remain enforced. An upload admitted while entitled can finish after revocation; subsequent read/publication is denied. Privileged maintenance/provider behavior is a trusted-administration boundary.
- No TTL cleanup or lease steals. Future garbage collection requires separate approval and a protocol that excludes delayed/ambiguous publishers. A naive age-based orphan sweep is unsafe.

## Legacy-only staged procedure (NOT the fresh sandbox apply path)

1. Confirm dashboard project **pwpklwihwmxdehfdsoij**, never production. Stage 1 interrupts old uploads and cannot safely be rolled back after publication. Obtain approval for that maintenance window first. Prefer obtaining provider drain instructions before starting the outage. If the rejected migration was ever applied outside this session, STOP: this is a replacement for an unapplied migration, not an automatic repair/drop script.
2. Via the sandbox Storage dashboard/API, create private `capture-image-candidates`, maximum file size **2250000 bytes**, allowed MIME exactly `image/png`, `image/jpeg`, `image/webp`, `image/gif`. This document is not an instruction to expose any credentials to an agent.
3. Ensure `20260913120000_capture_images.sql` is installed (private legacy bucket with the same size/MIME limits). Run ONLY `supabase/migrations/20260913220000_image_publication.sql` in sandbox SQL editor; do not `supabase db push` unrelated billing migrations. After successful commit, `select public.capture_image_publication_ready();` MUST return false. Record successful fresh old-route/direct-Storage upload denials. Stage 1 writes no legacy objects; it does not assert they are stable yet.
4. STOP HERE until provider drain and complete legacy preservation evidence are reviewed. `supabase/sandbox-image-publication-stage2.sql` is the exact separate activation template, with empty fields that deliberately abort. Only an authorized operator may fill reviewed report IDs and their name after the evidence exists. Then run it and `supabase/verify-image-publication.sql`: expect the app-owned guard enabled `O`. That read-only verification intentionally reports BLOCKED before stage 2. None of these checks establishes hosted byte safety automatically.
5. Use an already authenticated, synthetically entitled sandbox account at **http://127.0.0.1:4998/sandbox-image-publication-acceptance.html**. Click **Run five publication acceptance rounds** once. It verifies the exact sandbox public config, then runs five fresh HEAD404 → parallel PNG/GIF PUTs → two GETs. Every round must show exactly one `stored:true`, one `stored:false`, stable exact attributed bytes, no-store and HTTP success. 25 image requests; no retry or cleanup. Missing schema yields non-404 HEAD and no upload. No payment calls; entitlement is not granted by this harness.
6. Retain IDs/results. Re-read the last round with the second button; verify legacy inventory readbacks, account-B/expired denial, stale old route/direct Storage denial, same-owner tamper failure, and synthetic account-erasure via supported API. Repeat on the target hosted version. A passing batch alone does not prove drain, general legacy preservation, cleanup, another deployment or full UI sync. No production promotion until all those gates pass.

## Supported administrative account-erasure path

Normal users cannot delete Storage objects or publication rows. There is NO blanket trigger blocking administrative Storage API removal. Follow [Supabase Storage schema guidance](https://supabase.com/docs/guides/storage/schema/design): mutate files only through Storage API/dashboard, never SQL DELETE of `storage.objects`. [Auth deletion requires removing owned objects first](https://supabase.com/docs/guides/auth/managing-user-data#deleting-users).

For an approved account erasure, an operator/trusted backend (never privileged browser code) must: (1) block the owner's new image admissions/publications by revoking live entitlement and prevent billing reconciliation regrant during erasure; revoke sessions, noting existing JWTs can remain valid; (2) obtain/execute the same verified provider drain covering already-admitted uploads; (3) enumerate ALL owned paths in BOTH buckets including losing/orphan candidates using privileged API, paginate fully, remove via Storage API, verify empty with API plus read-only metadata inventory; include other owned buckets before Auth deletion; (4) delete user through Auth admin API/dashboard, read back deletion and verify publication FK cascade and no residual owned Storage objects. Retry failures, do not declare erasure complete on a partial batch or Auth error. Local SQL tests model only privileged metadata deletion plus FK cascade, NOT actual backend-byte erasure. **Hosted drain and end-to-end account-erasure remain launch gates.** No credentials or privileged cleanup endpoint added; no naive age-based orphan GC.

Rollback after cutover means keeping image operations unavailable while investigating. **Do not unfreeze legacy writes or revert to the old uploader** after new pointers exist; do not drop the bucket or publications. Other app features can keep operating.

## Local executable evidence

From the repository:

```sh
npm test -- --maxWorkers=2 src/lib/cloudImageRoute.test.ts src/lib/cloudImageMigration.test.ts src/lib/imgSync.test.ts src/lib/imgRoute.test.ts src/lib/hubStore.test.ts
python3 scripts/test-image-publication-sql.py
node scripts/test-image-publication-batch.cjs
npx tsc --noEmit --incremental false
npx eslint src/lib/cloudImage.ts src/lib/cloudImageRoute.test.ts src/lib/cloudImageMigration.test.ts scripts/fixtures/sandbox-image-publication-acceptance.js scripts/test-image-publication-batch.cjs
```

The route regression was RED against the old implementation (two `stored:true` results) and GREEN after durable publication. It runs two separately loaded handler modules against a shared provider-boundary simulator that admits both uploads. Additional tests cover crash before/after commit, legacy recovery, missing gate, dangling publication, valid-raster tampering and revocation during upload. These are mocked service-boundary tests, not hosted claims.

The Python test boots/destroys a fresh Unix-socket-only PostgreSQL cluster with an explicit non-secret environment and synthetic auth/Storage schema. It applies real migrations, the stage-2 template with LOCAL FIXTURE ONLY evidence, and verification SQL. Five independent psql-session INSERT races prove unique-key arbitration and retained winner. The revised test was RED against the rejected trigger migration and GREEN after replacement. It proves new legacy admissions fail, a pre-admitted privileged completion CAN finish while activation remains false, stage-2 blank evidence aborts, direct publication is blocked before activation, both original/late legacy IDs cannot be shadowed afterward, user pointer mutation is denied, and administrative cleanup's DB leg plus FK cascade is permitted. No sleep and no simulated claim that RLS drains a provider. SQL cannot prove hosted API compatibility, backend-byte safety or completed account erasure.

The Node batch test executes the actual browser script in a VM against synthetic HTTP behavior: correct publication, both-success regression, missing no-store, denied HEAD and network-read failure. It preserves all five round records on attribution/read failures and never PUTs after a non-404 prerequisite. It is not a live Supabase test. No shared `.next` build is run.

Latest local revision: **5 targeted Vitest files / 76 tests passed**, disposable PostgreSQL script passed (including blank stage-2 refusal), Node batch script passed all five scenarios, and project `tsc --noEmit --incremental false` passed. HEAD tests were observed RED on the previous full-download implementation, then GREEN on metadata-only HEAD with same-size tamper rejection retained on GET/PUT. Scoped ESLint exited 0 (invoking from the parent workspace emitted a Next pages-directory warning); `git diff --check` passed. No hosted test, remote migration, reset or deployment was performed. These results do not clear the drain, preservation or erasure gates.
